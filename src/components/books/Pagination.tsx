import styled from 'styled-components';

const Pagination = () => {
  return (
    <PaginationStyle>
      <div>Pagination</div>
    </PaginationStyle>
  );
};

const PaginationStyle = styled.div``;

export default Pagination;
