import styled from 'styled-components';

const BooksViewSwicher = () => {
  return (
    <BooksViewSwicherStyle>
      <div>BooksViewSwicher</div>
    </BooksViewSwicherStyle>
  );
};

const BooksViewSwicherStyle = styled.div``;

export default BooksViewSwicher;
